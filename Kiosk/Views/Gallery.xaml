<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Class="Kiosk.Views.Gallery"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Background>
        <SolidColorBrush Color="Black" Opacity="0.5"/>
    </UserControl.Background>
    <Grid>
        <ListBox x:Name="GalleryList" HorizontalContentAlignment="Center" Padding="50" ItemsSource="{Binding ImagesList}" Background="{x:Null}" BorderBrush="{x:Null}" >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel x:Name="WrapPanel" HorizontalAlignment="Center" Orientation="Horizontal" MaxWidth="{Binding ActualWidth, ElementName=GalleryList}"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Height="250" Width="250" Margin="10"  OpacityMask="{x:Null}">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding Source}" Stretch="Uniform"/>
                        </Border.Background>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
